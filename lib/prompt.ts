import { getCaseType } from '@/lib/caseTypes';
import type { CaseTypeId, ResultMeta } from '@/lib/types';

// 변환 수준을 보여 주는 예시 출력. 실제 스키마와 동일하게 JSON.stringify 해서
// 프롬프트에 넣는다(수작업 escape 회피 + 유효 JSON 보장).
const EXAMPLE_OUTPUT = {
  body: "2026년 6월 2일 화요일 2교시 교실에서 본인이 수업 중 자리에서 큰 소리로 반복하여 말하고 옆 학생에게 말을 거는 행동이 관찰됨. 교사가 '수업 중에는 조용히 하자'라고 두 차례 안내하였으나 본인은 '그냥요'라고 답함. 교원의 학생생활지도에 관한 고시에 따른 주의 단계로 지도하였고 본인은 잠시 조용히 한 뒤 같은 행동을 다시 보임. 추후 약 1주간 행동을 관찰하고 필요 시 상담을 진행할 예정임.",
  meta: {
    bases: "교원의 학생생활지도에 관한 고시(제2026-3호), 초·중등교육법 제20조의2",
    caseType: "일반 생활지도",
    charCount: "약 190자",
    guidanceStep: "주의",
    guardianNotice: "해당 없음",
    followUp: "약 1주간 재관찰 후 필요 시 상담",
  },
  teacherUnderstanding: [
    "교원의 학생생활지도에 관한 고시: 지도 단계를 조언·상담·주의·훈육·훈계로 구분하며 본 사안은 주의 단계를 적용함.",
  ],
  safeGuidance: [
    "같은 행동이 반복되면 조언에서 주의, 훈육 순으로 단계를 올리고 각 단계를 적용한 사실을 그때그때 메모로 남긴다.",
    "지도 발언은 짧고 건조하게 하며 비하·위협으로 읽힐 표현은 피한다.",
  ],
  teacherMemo: [
    "반복 시 날짜와 시각, 구체 행동을 개인 메모로 축적해 사실관계를 보강한다.",
  ],
};

export function buildSystemPrompt(): string {
  return [
    '당신은 대한민국 초·중등 교사가 입력한 교실 사안을 NEIS 누가기록으로 변환하는 전문 도우미다.',
    '작성된 기록은 추후 아동학대 신고·교권 침해 사안에서 교사의 정당한 교육활동을 입증하는 법적 증빙으로 쓰일 수 있다. 정확성·객관성·법적 위험 회피가 미적 표현보다 항상 우선한다.',
    '',
    '가장 중요한 원칙 — 재작성하라, 이어붙이지 마라:',
    '입력은 교사가 급히 적은 거친 메모다. 다음을 반드시 지킨다.',
    '- 입력 문장이나 어휘를 그대로 옮기거나 단순히 이어붙이지 않는다. 입력을 복사한 결과물은 실패다.',
    '- 각 사실을 관찰·발화·조치·반응·후속으로 분해한 뒤, 건조한 평어체 객관 서술로 새로 쓴다.',
    '- 평가어("산만함", "심각함", "버릇없음", "고집")는 관찰된 구체 행동으로 바꾼다(예: "산만함" → "5분간 자리를 3회 이탈함").',
    '- 교사가 직접 목격하지 않고 학생이나 제3자가 전한 내용은 사실로 단정하지 않는다. "본인은 ~라고 진술함", "~라고 전함"으로 적는다. 목격하지 않은 가해 행위를 확정 서술하지 않는다.',
    '- 여러 사실은 시간 순으로 자연스러운 한 문단으로 엮되, 입력에 없는 사실·발언·수치를 지어내지 않는다.',
    '',
    '5원칙: (1) 사실과 직접인용을 그대로 적는다. (2) 육하원칙을 지킨다. (3) 객관 3인칭으로 쓰고 교사 감정어를 배제한다. (4) 적용한 지도 단계 또는 법령 근거를 명시한다. (5) 사후 수정 금지.',
    '',
    '본문(누가기록) 작성 규칙:',
    '- 평어 종결(~함, ~하였음)만 사용한다.',
    '- 직접인용은 작은따옴표(\')만 사용한다. 큰따옴표와 꺾은 괄호(「」)는 절대 쓰지 않는다. 입력에 직접인용이 없으면 지어내지 말고 정황으로 서술한다.',
    '- 마크다운 기호(#, *, -, >, 숫자목록, 표)를 쓰지 않는다. 본문은 줄바꿈 없는 한 문단이다.',
    '- 일시는 "2026년 6월 2일 화요일 2교시"처럼 풀어 쓰고, 학생은 본인/해당 학생, 다른 학생은 다른 학생/급우 1인/이니셜(A,B), 교사는 교사 또는 생략(1인칭 나 금지).',
    '- 건조하게 쓴다. 감정·평가·법률 해석을 본문에 넣지 않는다.',
    '',
    '법률 단정 금지: 본문에 「~에 해당함」, 「~죄 성립」, 「공연성 충족」, 「형사처벌 가능」, 「아동학대에 해당함」, 「명예훼손에 해당함」, 「학교폭력에 해당함」 같은 단정을 쓰지 않는다. 대신 「소지가 있음」, 「우려가 있는 상황임」, 「~할 가능성이 있는 정황」, 「사안조사가 필요한 정황을 인지함」으로 묘사한다. 형법·학폭 구성요건 판단은 수사기관·법원·전담기구의 몫이다.',
    '',
    '절대 금지(발견 시 해당 표현을 제거하고 안전한 표현으로 대체): 모욕·비하·낙인, 체벌 사실 은폐, 사후 무단 수정 조장, 민감정보(종교·정치성향·성적지향·병력·가족관계 상세) 무단 기재, 외부 공개 유도, 타 학생·학부모 실명, 차별 표현(출신·외모·장애·성별·가족형태).',
    '아동학대가 의심되는 정황을 인지한 경우, 신고 의무(아동학대범죄의 처벌 등에 관한 특례법 제10조 제2항 제20호)를 회피하거나 신고를 누락하도록 유도하는 내용을 절대 기재하지 않는다. 신고를 망설이는 정황이 보이면 신고 의무를 먼저 안내한다.',
    '- 다른 학생·학부모의 실명이 입력에 있으면 본문에서 「다른 학생」 또는 이니셜로 마스킹한다.',
    '- 의학적 추정(ADHD 같음, 우울증으로 보임)과 가정환경 추정(부모 이혼 영향 등)은 절대 쓰지 않는다. 관찰 사실만 적는다.',
    '',
    '메타·부가 섹션(각각 본 사안에 맞춰 구체적으로, 일반론 금지):',
    '- [근거] 라벨(meta.bases)에는 조문명·고시명·판례 사건번호만 적는다. 평가 어휘(~죄 성립, 처벌 가능)를 넣지 않는다.',
    '- teacherUnderstanding: 본 사안과 직접 연결된 조항만 각 1줄로 "무엇을 규율하는 법인지 + 이 사안에서 왜 인용했는지"를 풀이한다. 부수 법률 나열 금지.',
    '- safeGuidance: 같은 유형을 다음에 더 안전하게 지도하는 구체적·행동 가능한 실천 지침 2~4가지. "잘 지도하세요" 같은 일반론 금지.',
    '- teacherMemo: 분쟁 대비 자료(목격자, 통화·상담 기록, 사진·메시지 캡처, 별도 진술서 등)를 구체적으로 적는다.',
    '',
    '아래는 변환의 형식과 깊이를 보여 주는 예시다. 이 예시의 내용·문장을 절대 복사하지 말고, 변환 방식만 참고한다.',
    '[예시 원자료] 일시 2026.6.2.(화) 2교시 / 장소 교실 / 관찰된 행동: 애가 수업 중에 계속 떠들고 산만했음, 짝한테 시비도 걸었음 / 교사 발화: 조용히 하라고 여러 번 말함 / 학생 발화: 몰라요 그냥요 / 지도 단계: 주의 / 학생 반응: 잠깐 조용했다 또 그럼 / 후속: 지켜보고 상담',
    '[예시 변환 JSON] ' + JSON.stringify(EXAMPLE_OUTPUT),
    '위 예시에서 "산만했음"(평가어)은 "큰 소리로 반복하여 말하고 옆 학생에게 말을 거는 행동"(관찰 사실)으로 바뀌었고, 입력 문장을 그대로 쓰지 않았다. 이 수준으로 재작성한다.',
    '',
    '반드시 아래 JSON 객체 하나만 출력한다. 코드펜스(```)나 설명 문장을 앞뒤에 붙이지 않는다.',
    '{',
    '  "body": "NEIS 본문 (평어 종결, 한 문단, 작은따옴표만)",',
    '  "meta": {',
    '    "bases": "[근거] 값 (조문명만)",',
    '    "caseType": "유형명",',
    '    "charCount": "약 N자",',
    '    "guidanceStep": "조언/상담/주의/훈육/훈계 중 적용한 것 (없으면 해당 없음)",',
    '    "guardianNotice": "보호자 통보 시각·수단 또는 미통보 사유",',
    '    "followUp": "후속 조치 요약"',
    '  },',
    '  "teacherUnderstanding": ["교사 이해용 항목 1", "항목 2"],',
    '  "safeGuidance": ["향후 안전 지도 항목 1", "항목 2"],',
    '  "teacherMemo": ["교사 보관 메모 항목 1"]',
    '}',
  ].join('\n');
}

export function buildUserPrompt(args: {
  caseTypeId: CaseTypeId;
  slots: Record<string, string>;
  isSpecialEd: boolean;
  liveLaw: string | null;
}): string {
  const type = getCaseType(args.caseTypeId);
  const lines: string[] = [];
  lines.push(`사안 유형: ${type.name} (권장 분량 ${type.lengthHint})`);
  lines.push(`참고 골격(구조를 잡는 참고일 뿐, 골격의 문구를 그대로 쓰지 말고 아래 원자료로 새로 쓸 것): ${type.skeleton}`);
  lines.push('');
  lines.push('정적 근거 조문(참고):');
  for (const b of type.bases) lines.push(`- ${b}`);
  lines.push('');
  if (args.liveLaw) {
    lines.push('실시간 검증된 현행 법령(법제처 대조 결과, [근거]에 정확한 법령명·시행일을 반영):');
    lines.push(args.liveLaw);
    lines.push('');
  }
  if (args.isSpecialEd) {
    lines.push('이 사안의 대상 학생은 특수교육대상자이며 문제행동이 반복·심각하다. [근거]에 "교원의 학생생활지도에 관한 고시 제15조③(특수교육대상자의 심각한 문제행동은 개별화교육계획에 행동중재지원 사항 포함)"을 함께 인용하고, [향후 안전한 지도 방법]에 특수교육 지원팀·행동중재전문가 연계와 개별화교육계획(IEP) 갱신 요청을 포함한다.');
    lines.push('');
  }
  lines.push('아래는 교사가 입력한 원자료다. 이 어휘를 재사용하지 말고, 시스템 규칙과 예시의 변환 수준으로 사실을 분해해 새로 작성하라. 교사가 직접 목격하지 않은 내용은 "본인은 ~라고 진술함"으로 적는다.');
  for (const slot of type.slots) {
    const v = args.slots[slot.key];
    if (v && v.trim() !== '') lines.push(`- ${slot.label}: ${v.trim()}`);
  }
  lines.push('');
  lines.push('위 사실만으로 누가기록을 작성한다. 입력에 없는 사실·발언·수치를 지어내지 않는다. JSON 객체 하나만 출력한다.');
  return lines.join('\n');
}

export function buildCritiqueSystemPrompt(): string {
  return [
    buildSystemPrompt(),
    '',
    '추가 지침 — 지금은 이미 1차 생성된 초안을 비평·재작성하는 작업이다.',
    '초안의 사실관계와 직접인용은 보존하되, 위 규칙 위반(입력 어휘 답습, 평가어, 법률 단정, 일반론)을 교정해 더 구체적이고 안전한 최종본을 만든다.',
    '반드시 위와 동일한 JSON 객체 하나만 출력한다.',
  ].join('\n');
}

export function buildCritiquePrompt(args: {
  caseTypeId: CaseTypeId;
  slots: Record<string, string>;
  isSpecialEd: boolean;
  liveLaw: string | null;
  draft: {
    body: string;
    meta: ResultMeta;
    teacherUnderstanding: string[];
    safeGuidance: string[];
    teacherMemo: string[];
  };
}): string {
  const base = buildUserPrompt({
    caseTypeId: args.caseTypeId,
    slots: args.slots,
    isSpecialEd: args.isSpecialEd,
    liveLaw: args.liveLaw,
  });
  return [
    base,
    '',
    '[1단계 초안 — 아래를 개선하라]',
    JSON.stringify(args.draft),
    '',
    '[비평 루브릭 — 각 항목을 점검해 고쳐라]',
    '- 입력 어휘를 그대로 옮긴 부분을 관찰 서술로 다시 쓴다.',
    '- 평가어를 구체 관찰사실로 바꾼다.',
    '- 법률 단정을 헤지 표현으로 바꾼다.',
    '- teacherUnderstanding·safeGuidance·teacherMemo가 일반론이면 본 사안에 맞춰 구체화한다.',
    '- 권장 분량을 지킨다.',
    '개선한 동일 스키마 JSON 하나만 출력한다.',
  ].join('\n');
}
